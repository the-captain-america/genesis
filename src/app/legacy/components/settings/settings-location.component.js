import locationController from './settings-location.controller';

const locationComponent = {
    controller: locationController,
    template: require('./settings-location.template.html')
};

export default locationComponent;
