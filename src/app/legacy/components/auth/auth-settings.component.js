import loginController from './auth-settings.controller';

const loginComponent = {
    controller: loginController,
    template: require('./auth-settings.template.html')
};

export default loginComponent;
